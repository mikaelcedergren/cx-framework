interface:
  display_name: "Copywriter"
  short_description: "Clear, concise product copy"
  default_prompt: "Use $copywriter to write or improve clear UI copy, validation, alerts, empty states, and guidance without inventing product behavior."
policy:
  allow_implicit_invocation: true
