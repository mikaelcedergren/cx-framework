interface:
  display_name: "Developer"
  short_description: "Scoped, verified implementation"
  default_prompt: "Use $developer to implement an approved scope, preserve existing work, fix the owning layer, and verify claims proportionally to risk."
policy:
  allow_implicit_invocation: true
